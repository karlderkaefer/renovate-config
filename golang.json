{
  "extends": [
    "config:base",
    "group:monorepos"
  ],
  "prHourlyLimit": 10,
  "rebaseWhen": "auto",
  "labels": ["dependencies"],
  "packageRules": [
    {
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "automerge": true
    },
    {
      "depTypeList": ["devDependencies"],
      "automerge": true
    }
  ]
}
